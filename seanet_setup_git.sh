#!/bin/bash

git config --global push.default simple
git config --global user.email pats0nn@gmail.com
git config --global user.name Alex Safonov
git config --global credential.helper "cache  --timeout 33600"
